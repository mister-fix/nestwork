import { account, session, user, verification } from './auth-schema';

export { account, session, user, verification } from './auth-schema';
export const schema = { account, user, verification, session };
